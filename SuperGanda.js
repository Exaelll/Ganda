function toggleGrid() {
    var gridContainer = document.querySelector('.wrap');
    var headerButton = document.querySelector('.Header');

    // Toggle class to move gridContainer
    gridContainer.classList.toggle('slide-down');
}